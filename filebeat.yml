filebeat.inputs:
  - type: filestream
    id: osquery
    paths:
      - /var/log/osquery/osqueryd.results.log
    parsers:
      - ndjson:
          overwrite_keys: true

output.elasticsearch:
  hosts: ["localhost:9200"]

setup.kibana:
  host: "localhost:5601"